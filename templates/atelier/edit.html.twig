
{% extends 'base.html.twig' %}

{% block title %}Edit Atelier{% endblock %}

{% block body %}
    <h1>Modifier Atelier</h1>

    	{{ form_start(form) }}
	{{ form_widget(form) }}
	<button class="btn">{{ button_label|default('Update') }}</button>
	{{ form_end(form) }}

    <a href="{{ path('atelier_index') }}">back to list</a>

    {{ include('atelier/delete.html.twig') }}
{% endblock %}